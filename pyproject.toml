[project]
name = "ansible-role-memos"
version = "0.0.0"
description = "Simple ansible role for running dockerized memos"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ansible==14.3.1",
    "ansible-lint==26.8.0",
    "black==26.5.1",
    "jmespath==1.1.0",
    "yamllint==1.38.0",
]

[tool.uv]
package = false
